import logging

logger = logging.getLogger()
logger.setLevel(logging.INFO)


def lambda_handler(event, context):

- react to a dynamo stream and do something with the GET activity call...
    put somewhere that it can be render by Streamlit.io 

    